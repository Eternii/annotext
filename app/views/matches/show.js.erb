lastclicked = "<%= @match_string %>";

$("#current-match").html("<%= @match_string %>");

$("#gloss-count").html("(<%= pluralize(@gloss_entries.count, 'match') %>)");
$("#dict-count").html("(<%= pluralize(@dict_entries.count, 'match') %>)");

$("#gloss-definitions").html("");
$("#gloss-definitions").append("<%= escape_javascript(render partial: 'shared/entry', collection: @gloss_entries) %>");

$("#new-definition").html("");
$("#new-definition").append("<%= escape_javascript(render partial: 'shared/new_glossary_def') %>");

$("#dict-definitions").html("");
$("#dict-definitions").append("<%= escape_javascript(render partial: 'shared/entry', collection: @dict_entries) %>");

$("#clicked-phrase").html("");
