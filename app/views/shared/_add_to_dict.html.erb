<%= form_tag(copy_to_dict_definition_path(entry.id), method: 'post', remote: true, class: "add-form") do %>
  <%= hidden_field_tag "current_match", @match_string %>
  <%= submit_tag "Add to dictionary", class: "btn btn-xs btn-primary" %>
<% end %>
