<%= form_tag(copy_to_gloss_definition_path(entry.id), method: 'post', remote: true, class: "add-form") do %>
  <%= hidden_field_tag "text_id", @text_id %>
  <%= submit_tag "Add to glossary", class: "btn btn-xs btn-primary" %>
<% end %>
